/**
 * Shared JSONPath utilities.
 * Re‑exported from @reqly/shared to eliminate duplication.
 */
export {
  resolveJsonPath,
  getValueByPath,
  tryParseJson,
  isSourcePathSyntaxValid,
} from "@reqly/shared/variable-path";
