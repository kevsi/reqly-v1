---
title: Tools Overview
description: The 44 MCP tools, grouped by purpose.
icon: Wrench
---

All tools are JSON-RPC `tools/call` invocations with a JSON-Schema `inputSchema` (as served by `tools/list`). Results are returned as formatted JSON text. Tools that cannot satisfy a call return `isError: true` with a human-readable message instead of throwing.

Tools are grouped here for readability:

## Collections
[`list_collections`](../tools/collections) · [`get_collection_tree`](../tools/collections) · [`create_collection`](../tools/collections) · [`update_collection`](../tools/collections) · [`delete_collection`](../tools/collections) · [`duplicate_collection`](../tools/collections)

## Requests
[`list_requests`](../tools/requests) · [`get_request`](../tools/requests) · [`create_request`](../tools/requests) · [`update_request`](../tools/requests) · [`delete_request`](../tools/requests) · [`duplicate_request`](../tools/requests) · [`move_request`](../tools/requests) · [`reorder_requests`](../tools/requests) · [`search_requests`](../tools/requests) · [`import_from_curl`](../tools/requests) · [`export_request_to_curl`](../tools/requests) · [`validate_request`](../tools/requests) · [`generate_request_from_description`](../tools/requests)

## Folders
[`create_folder`](../tools/folders) · [`update_folder`](../tools/folders) · [`delete_folder`](../tools/folders)

## Execution
[`run_request`](../tools/execution) · [`run_collection`](../tools/execution) · [`run_requests_batch`](../tools/execution) · [`run_collection_with_assertions`](../tools/execution) · [`get_request_history`](../tools/execution) · [`get_run_history`](../tools/execution)

## Environments
[`list_environments`](../tools/environments) · [`create_environment`](../tools/environments) · [`update_environment`](../tools/environments) · [`delete_environment`](../tools/environments) · [`duplicate_environment`](../tools/environments) · [`get_environment_variables`](../tools/environments) · [`resolve_variables`](../tools/environments)

## Import / Export
[`export_bundle`](../tools/import-export) · [`import_bundle`](../tools/import-export) · [`import_from_openapi`](../tools/import-export) · [`export_to_openapi`](../tools/import-export) · [`export_collection_to_junit`](../tools/import-export)

## API Intelligence
[`generate_tests`](../tools/intelligence) · [`openapi_sync`](../tools/intelligence) · [`analyze_project_routes`](../tools/intelligence) · [`graphql_execute`](../tools/intelligence)

---

## Conventions

**IDs.** Collections, requests, folders and environments are identified by stable IDs (`col-…`, `req-…`, `env-…`, `fld-…`). Execution tools also accept a request's unique **name** as `request_id`, so agents can use human-readable references. Get IDs from `list_collections`, `list_requests`, `get_collection_tree`, or the MCP resources.

**Optional `env_name`.** Execution tools accept an `env_name`; when omitted they use the server's `--env` default, then bundle variables.

**Errors.** A failed lookup returns something like `Collection not found: col-xyz` with `isError: true`, so agents can recover gracefully.

**Timeouts.** Execution tools accept `timeout_ms`; the server default is 30 s.
